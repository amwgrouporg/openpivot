# OpenPivot follow-up red-team, Fable 5, 2026-09-03

Scope: uncommitted hardening diff (findings 26-31 in docs/REDTEAM_gpt56sol_20260902.md).

1. HIGH | src/sensors/extract.js:78 (BLOCK at :42 includes `br`) | `e.onEndTag()` on the void element `br` throws inside lol-html ("Parser error: No end tag."), so `extractFromHtml` throws on any page containing `<br>`, and because the parse call at extract.js:131 sits outside the sensor's try/catch the failure escapes the envelope as a 500 with `sensor: "api"` and `source_url: null`; `extract_page` and `pivot_url` are dead on most real pages and tests/extract.test.js never feeds a void block element, so 225/225 still pass | `GET /api/extract?url=https://www.kernel.org/` (3 `<br>`) -> `{"status":"indeterminate","sensor":"api","source_url":null,"error":"worker error: Parser error: No end tag."}`; `https://www.debian.org/` same; `https://example.com/` (no `<br>`) -> ok | Register the end-tag handler only for non-void block elements (emit `br` on its start tag alone), move `extractFromHtml` inside the sensor's indeterminate path so a parser fault keeps `sensor: "extract"` and the source URL, and add a `<p>a<br>b</p>` fixture.
2. HIGH | src/worker.js:161, wrangler.toml `[assets]` (no `run_worker_first`), src/headers.js | `withSecurityHeaders` never runs for the board: with the default `run_worker_first = false` the asset layer answers every request that matches a file (`/`, `/index.html` -> 307, `/demo/injected`, `/styles.css`, `/vendor/*`, the ETag 304 revalidations) before the Worker is invoked, so the CSP, referrer-policy, x-frame-options and nosniff are attached only to non-asset paths; the README's "Pages are served with a Content-Security-Policy" and the finding-30 "Fixed" row are false in production, and tests/headers.test.js passes because it substitutes a stub ASSETS binding | `wrangler dev` -> `curl -D - http://127.0.0.1:8799/` and `/app.js` carry no content-security-policy / x-content-type-options; `curl -D - http://127.0.0.1:8799/no/such/path` (SPA fallback through the Worker) carries all four | Ship `public/_headers` with the same policy for `/*` (Workers static assets honour it for asset responses; Worker-generated fallbacks keep `withSecurityHeaders`) or set `run_worker_first = true`, and replace the stub-binding test with one that reads the served headers from a real workerd via the extract-harness pattern.
3. LOW | src/limiter_do.js:21, :33 | The loaded hit list is not validated: a stored value that is not an array becomes `[]` and non-numeric entries are filtered away as NaN, so a corrupt `hits` key opens the window instead of refusing, while a future timestamp (clock skew after a machine move) blocks the key until it ages out and reports retry_after far beyond the window | `storage.get` -> `"junk"` -> `{allowed:true, remaining:2}`; `storage.get` -> `[now+3600000 x3]` with limit 3 -> `{allowed:false, retry_after:3660}` | Throw when the value is not an array of finite numbers, drop entries greater than `now`, and clamp retry_after to `ceil(windowMs/1000)`.
4. LOW | src/validate.js:67-73 | `ipv6Groups` accepts an embedded dotted quad that is not the last group when a `::` follows it, returning a "valid" parse for invalid text and reporting it public; unreachable today because `parseHttpUrl` and `assertResolvesPublic` only hand it WHATWG-canonicalised literals, but the exported parser is wrong for any direct caller | `ipv6Groups("1.2.3.4::")` -> `102:304:0:0:0:0:0:0`, `isPrivateIPv6("1:2:1.2.3.4::")` -> false (expected null / true) | Permit the dotted quad only when it is the final group of the whole address (tail non-empty, or no `::`).
5. PLAUSIBLE | tests/extract.test.js:18-19 | When the 30 s deadline wins the race the rejected `before` fails the file loudly (verified for a throwing hook), but the still-pending `unstable_dev` startup is neither awaited nor stopped, so a slow-but-successful start leaves a workerd running with `worker` undefined, `after()` a no-op, and the test process possibly unable to exit | startup resolving at 31 s -> `after()` does nothing, dev server keeps the child alive | Keep the startup promise and `.then((w) => w.stop())` it on deadline.
6. PLAUSIBLE | src/headers.js:4-15 | The ChatGPT desktop browser's WebMCP bridge could not be exercised here; if it injects page-context `<script>` or inline handlers, `script-src 'self'` blocks it (Chrome 149 with the flag verified clean: 10 tools plus the dynamic `pivot_domain` registered, zero CSP violations) | unverified | Load the board once in the ChatGPT desktop browser after finding 2 is fixed and check the console for CSP reports.

Verified by: `npm test` twice (225/225, 2.2-2.5 s, `.wrangler/tmp` left empty); Node probes of `ipv6Groups`/`isPrivateIPv6` on 70 inputs against the WHATWG parser (no public address wrongly refused on any canonical form; every caller of `isPrivateIPv6` pre-filters through the URL parser, so the unparseable -> refused change breaks no caller, and `assertResolvesPublic` still refuses `::ffff:7f00:1`, `64:ff9b::7f00:1`, `2002:c0a8:101::`); Node probes of `Limiter` (rejected `get` is retried on the next call, a failed `put` leaves `this.hits` equal to storage, corrupt values as in finding 3); a scratchpad workerd worker binding the real `Limiter` Durable Object (100 and 300 concurrent calls -> exactly 60 allowed, sequential identical, so input gates hold), the real ASSETS binding through `withSecurityHeaders` (304 has a null body and re-wraps without throwing; HEAD and HTML fine; `new Response(stream, {status:304})` throws in workerd, so the null-body invariant matters), and `extractFromHtml` with `<br>` (throws) versus nested/implied end tags (fine); `wrangler dev` of the real worker: header probe on 8 paths and `/api/extract` on kernel.org, debian.org and example.com; Chrome 149 with the WebMCP flag on the CSP-carrying `/board` path: `list_webmcp_tools`, `add_entity` executed, `pivot_domain` appeared on toolchange, Entities view rendered one D3 node with the inline `--type-color` style honoured, console free of CSP violations. `blob:` anchor downloads are not governed by any CSP directive and were not click-tested. The undo change (#31) was read against `removeEntity`/`restoreRemoval`/`mergeGraphPositions`/`createPositionPublisher`: `flushPositions` runs before removal, publishes after removal cannot touch the deleted id, and restore re-inserts the snapshot position; no defect found. No file in the repo was modified other than creating this report.
VERDICT: do not ship -- the extractor now throws on any page containing `<br>`, and the CSP is never attached to the board page because matched assets bypass the Worker.

---

## Verification pass (second)

Working tree re-verified after the fixes for findings 1-5. Method: `npm test` (five runs, tests 231 pass 231 fail 0 each, 2.2-2.9 s); `wrangler dev` of the real worker with `run_worker_first = true` (fresh persisted state); a mutation of each fix with the affected test file re-run, files restored from copies and confirmed byte-identical by sha256 (the fixes are uncommitted, so `git checkout` was not used); a scratchpad asset-first worker for the Range comparison; the 70-input IPv6 probe re-run against the tightened parser (0 canonical forms wrongly refused).

1. Fixed. `br` has its own start-tag-only selector, BLOCK no longer lists it, and the parse sits inside the sensor's indeterminate path. Live: `/api/extract?url=https://www.kernel.org/` -> `status: "ok"`, `sensor: "extract"`, title "The Linux Kernel Archives", 1676 chars, 91 links; debian.org likewise; example.com unchanged. Mutation: putting `br` back on `onEndTag` fails "void block elements such as br ..." (8/9); removing the try/catch fails tests/extract_sensor.test.js (0/1).
2. Fixed. With `run_worker_first = true` every path now carries the policy: `/`, `/demo/injected`, the SPA fallback `/no/such/path` and `/api` (no slash) get CSP + referrer-policy + x-frame-options + nosniff; `/index.html` and `/demo/injected.html` still 307 with nosniff; `/styles.css`, `/app.js`, `/vendor/d3.v7.9.0.min.js` get nosniff only; HEAD `/` carries the policy; ETag revalidation returns 304 on both `/app.js` and `/` without throwing; served bodies are byte-identical to `public/` (sha256 of app.js, byte counts of d3 and index.html); POST/OPTIONS on `/` -> 405 as before; Range -> 200 full body, identical on an asset-first worker, so not a regression. Rate-limiter scope: 70 requests to `/app.js` then `/api/queries` -> 200; the API limit still trips at exactly the 61st API call of the minute with `Retry-After: 55`. Mutation: `run_worker_first = false` fails "the board page as served by the asset layer carries the policy" (3/4), so the new test detects the asset-layer bypass.
3. Fixed. A stored value that is not a list of finite numbers throws (Worker reads it as limited); entries after `now` are dropped; `retry_after` is capped at the window. Mutations: corrupt-list-as-empty fails its test (7/8); counting future timestamps fails its test (7/8); removing the `Math.min` cap fails nothing (8/8), so the cap is unproven by the suite. Note: a corrupt key stays refused for that client until the Durable Object's storage is cleared by hand, since the key is only rewritten on an allowed call; that is the fail-closed choice and it is documented in the file.
4. Fixed. `ipv6Groups("1.2.3.4::")` and `("1:2:1.2.3.4::")` -> null and `isPrivateIPv6` -> true; `::ffff:1.2.3.4`, `::1.2.3.4` and `1:2:3:4:5:6:1.2.3.4` still parse; no canonical form the URL parser can produce is refused. Mutation: reverting the `end` selection fails the new test (9/10).
5. Regressed (LOW, test files only). The deadline-wins case is now handled, but the timer is never cleared when startup wins: `Promise.race` settles, the 30 s `setTimeout` stays armed, and when it fires it runs `startup.then((w) => w.stop())` on the worker that started in time. Probe (timers scaled to 100 ms / 300 ms): stop() called at the deadline although startup had won. In the real files the timer is unref'd, so it only bites when a test file is still running 30 s after `before` began (slow CI, a debugger), and then it kills the workerd under the running tests with an unrelated fetch error instead of the "did not start" message. Fix: keep the timer id and `clearTimeout` it once the race resolves with the worker (a `finally` after the `await`).
6. Open, unchanged: the ChatGPT desktop browser check is on the submission checklist; nothing more can be verified here.

New since the fixes, none above LOW:
7. LOW | src/worker.js:162 | With `run_worker_first = true` the non-API branch now runs for every asset request and has no try/catch, so any throw there (an ASSETS binding failure, a future fault in headers.js) surfaces as a raw Worker exception for the whole board, something the asset-first default could not do | `env.ASSETS.fetch` rejecting -> unhandled exception page instead of a 5xx envelope or plain text | Catch around the asset branch and return a plain 503 text response; add a stub-binding test for it.
8. LOW | src/limiter_do.js:45 | The `retry_after` cap is unproven by the suite (mutation M3c passes) | remove `Math.min(...)` -> 8/8 still pass | Add a test with `hits[0]` skewed so the uncapped value would exceed `ceil(windowMs/1000)`.

VERDICT: ship -- findings 1-4 are fixed and proven by live repro and mutation; the only open items are LOW (an uncleared deadline timer in the two workerd test files, an untested retry_after cap, and the exception exposure that run_worker_first brings to the asset path).
