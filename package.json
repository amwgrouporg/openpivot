{
  "name": "openpivot",
  "version": "0.1.0",
  "private": true,
  "description": "An analyst-led investigation board over the open web's public record, exposed through WebMCP.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
